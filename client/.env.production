# Production build points the frontend at the live Catalyst AppSail backend
# (a running Node server querying the SQLite database), not the static snapshot.
VITE_CATALYST_API_URL=https://kspapi-50044156287.development.catalystappsail.in
